---
interface Props {
  src: any;
  class?: string;
  width?: string;
  height?: string;
}

const { src, class: className, width, height } = Astro.props;
---

<img src={src.src || src} alt="" class={className} style={`width: ${width}; height: ${height}`} />